import quangjs from '../src';
import * as qjs from '../src'
describe('basic import stuff', () => {
  it('default import/export', () => {
    console.log(quangjs)
  });
  it('keyvals', () => {
    console.log(qjs)
  });
});
